{
  "name": "electron-starter",
  "version": "0.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "browserify bmain.js -o public/bundle.js --node && electron ."
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "gl": "^2.1.5",
    "regl": "0.0.0"
  }
}
